name: Unit and Integration Tests x
on: 
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-2025, macos-latest]
    runs-on: ${{ matrix.os }}
    env:
      RUNNER_TEMP: ${{ startsWith(matrix.os, 'windows') && 'D:\\workspace\\runner_temp' || '' }}
      GOTMPDIR: ${{ startsWith(matrix.os, 'windows') && 'D:\\workspace\\go_tmp' || '' }}
      GOMODCACHE: ${{ startsWith(matrix.os, 'windows') && 'D:\\workspace\\go_mod_cache' || '' }}
      GOPATH: ${{ startsWith(matrix.os, 'windows') && 'D:\\workspace\\go_path' || '' }}

    steps:
      - name: Set working directory to D drive if Windows
        if: startsWith(runner.os, 'Windows')
        run: |
          mkdir D:\workspace
          xcopy /E /H /C /I . D:\workspace
          cd D:\workspace
          mkdir D:\workspace\runner_temp
          mkdir D:\workspace\go_tmp
          mkdir D:\workspace\go_mod_cache
          mkdir D:\workspace\go_path

      - name: Check out code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: "go.mod"
          cache: false

      - name: Print RUNNER_TEMP environment variable
        run: echo "RUNNER_TEMP is $RUNNER_TEMP"

      - name: Download dependencies
        run: go mod download
        env:
          GOMODCACHE: ${{ startsWith(matrix.os, 'windows') && 'D:\\workspace\\go_mod_cache' || '' }}
          GOPATH: ${{ startsWith(matrix.os, 'windows') && 'D:\\workspace\\go_path' || '' }}
          GOTMPDIR: ${{ startsWith(matrix.os, 'windows') && 'D:\\workspace\\go_tmp' || '' }}

      - name: Run unit and integration tests
        run: go test -race -tags=integration ./...
        env:
          GOMODCACHE: ${{ startsWith(matrix.os, 'windows') && 'D:\\workspace\\go_mod_cache' || '' }}
          GOPATH: ${{ startsWith(matrix.os, 'windows') && 'D:\\workspace\\go_path' || '' }}
          GOTMPDIR: ${{ startsWith(matrix.os, 'windows') && 'D:\\workspace\\go_tmp' || '' }}

      - name: Build
        run: go build -v ./cmd/gh

  integration-tests:
    env:
      GH_TOKEN: ${{ github.token }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    runs-on: ${{ matrix.os }}

    steps:
      - name: Check out code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: "go.mod"
          cache: false
          

      - name: Build executable
        run: make

      - name: Run attestation command set integration tests
        shell: bash
        run: |
          ./test/integration/attestation-cmd/run-all-tests.sh "${{ matrix.os }}"
